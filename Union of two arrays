#include <bits/stdc++.h>
using namespace std;

int main() {
	int test;
	cin>>test;
	for(int t=0;t<test;t++)
	{
	    int n,m;
	    cin>>n>>m;
	    int a[n],b[m];
	    	    set<int> a1,a2;
	    	     set<int>::iterator itr1,it2;
	    for(int i=0;i<n;i++)
	    {
	        cin>>a[i];
	        a1.insert(a[i]);
	    }
	        for(int i=0;i<m;i++)
	    {
	        cin>>b[i];
	        a1.insert(b[i]);
	    }

	        cout<<a1.size()<<" ";

 cout<<endl;
	    
	}
	return 0;
}
